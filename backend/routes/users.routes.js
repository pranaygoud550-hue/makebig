import { Router } from "express";
// import * as usersController from "../controllers/users.controller.js";
// import { authMiddleware } from "../middleware/auth.js";

const router = Router();

// TODO: move handlers from legacy.routes.js
// router.post("/upsert", usersController.upsert);
// router.get("/:contact", usersController.getByContact);
// router.get("/:userId/notifications", authMiddleware, usersController.listNotifications);

export default router;
