import { Router } from "express";
// import * as projectsController from "../controllers/projects.controller.js";
// import { authMiddleware } from "../middleware/auth.js";

const router = Router();

// TODO: move handlers from legacy.routes.js
// router.post("/create", authMiddleware, projectsController.create);
// router.get("/", authMiddleware, projectsController.listMine);
// router.get("/:projectId", authMiddleware, projectsController.getOne);
// router.post("/:projectId/publish", authMiddleware, projectsController.publish);

export default router;
