import { Router } from "express";
// import * as invitesController from "../controllers/invites.controller.js";
// import { authMiddleware } from "../middleware/auth.js";

const router = Router();

// TODO: move handlers from legacy.routes.js
// router.get("/sent", authMiddleware, invitesController.listSent);
// router.post("/send", authMiddleware, invitesController.send);
// router.post("/:inviteId/accept", authMiddleware, invitesController.accept);
// router.post("/:inviteId/decline", authMiddleware, invitesController.decline);

export default router;
